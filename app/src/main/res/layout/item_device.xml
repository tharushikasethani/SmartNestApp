<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="180dp"
    android:layout_height="185dp"
    android:layout_margin="8dp"
    android:layout_gravity="center_horizontal"
    app:cardBackgroundColor="#FFFFFF"
    app:cardCornerRadius="24dp"
    app:cardElevation="3dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp">

        <!-- Device Title at Top-Left -->
        <TextView
            android:id="@+id/tvDeviceName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_alignParentTop="true"
            android:layout_toStartOf="@id/ivDeviceIcon"
            android:fontFamily="sans-serif-medium"
            android:text="CCTV Camera"
            android:textColor="#9CA0A2"
            android:textSize="13sp"
            android:lineSpacingExtra="2dp" />

        <!-- Status Pill Optional (hidden) -->
        <TextView
            android:id="@+id/tvDeviceStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="gone" />

        <!-- Device Type Product Image in Center/Right -->
        <ImageView
            android:id="@+id/ivDeviceIcon"
            android:layout_width="85dp"
            android:layout_height="85dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:adjustViewBounds="true"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_camera1" />

        <!-- Subtitle / Location at Bottom-Left -->
        <TextView
            android:id="@+id/tvDeviceSubtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_alignParentStart="true"
            android:layout_toStartOf="@id/togglePill"
            android:layout_marginEnd="4dp"
            android:fontFamily="sans-serif"
            android:text="Main Gate"
            android:textColor="#8B8B8D"
            android:textSize="12sp"
            android:maxLines="1"
            android:ellipsize="end" />

        <!-- Toggle Switch Pill at Bottom-Right -->
        <FrameLayout
            android:id="@+id/togglePill"
            android:layout_width="42dp"
            android:layout_height="24dp"
            android:layout_alignParentBottom="true"
            android:layout_alignParentEnd="true"
            android:background="@drawable/bg_toggle_pill">

            <ImageView
                android:id="@+id/toggleDot"
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:layout_gravity="center_vertical|start"
                android:layout_margin="3dp"
                android:background="@drawable/bg_toggle_dot" />
        </FrameLayout>

    </RelativeLayout>

</androidx.cardview.widget.CardView>