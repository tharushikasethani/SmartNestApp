<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.SmartNest" parent="Theme.MaterialComponents.Light.NoActionBar">

        <item name="colorPrimary">@color/orange_primary</item>
        <item name="colorPrimaryDark">@color/orange_dark</item>
        <item name="colorAccent">@color/orange_primary</item>

        <item name="colorOnPrimary">@color/white</item>

        <!-- Light status bar with dark icons -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>

        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>

        <item name="android:windowBackground">@color/bg_screen</item>

    </style>

    <style name="Theme.SmartNest.Splash" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/splash_orange</item>
        <item name="android:statusBarColor">@color/splash_orange</item>
    </style>

    <!-- Dialog-compatible theme for MaterialTimePicker -->
    <style name="ThemeOverlay.SmartNest.TimePicker" parent="ThemeOverlay.MaterialComponents.TimePicker">
        <item name="colorPrimary">@color/orange_new</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSurface">@color/white</item>
    </style>

</resources>